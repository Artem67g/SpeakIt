# Normalise line endings so the repo is not full of CRLF/LF churn.
* text=auto eol=lf

# PowerShell is happier with CRLF on Windows, and cmd can misread a batch
# file without it.
*.ps1 text eol=crlf
*.bat text eol=crlf

*.png binary
